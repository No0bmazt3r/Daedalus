fastapi>=0.115
uvicorn[standard]>=0.30

# Thin HTTP client for the ChromaDB service (Layer 5). This is deliberately
# `chromadb-client` and not the full `chromadb`: the container talks to the
# chromadb compose service over HTTP, and the full package drags in
# onnxruntime/tokenizers for embedded mode — hundreds of MB the image never
# uses. For embedded mode (no CHROMA_URL), install `chromadb` instead.
# Either package satisfies the lazy import in app/db/vector_store.py.
chromadb-client>=0.5

# HTTP client for the connection test in app/services/model_endpoints.py.
# It arrives transitively via chromadb-client, but this module imports it
# directly and a transitive dependency is not a contract — a Chroma release
# that dropped it would break the Settings panel with a confusing error.
httpx>=0.27

# Hardware detection for the Forge (app/services/hardware.py). RAM, CPU cores
# and disk; GPU comes from pynvml or nvidia-smi when either is present, and
# neither is required — a CPU-only machine is a supported configuration.
psutil>=5.9

# NVIDIA's own NVML binding — the `pynvml` module `hardware._gpus()` prefers.
# Not for capability but for cost: the nvidia-smi fallback is a subprocess, and
# under WSL a Windows interop call, measured at 613ms against 13ms for this.
# That difference is what lets VRAM sit in the 30-second refresh tier instead
# of the five-minute one (see the snapshot cache section in hardware.py).
#
# Pure Python, ~53KB, no CUDA toolkit. It binds to libnvidia-ml.so.1 at call
# time, so on a machine or container without an NVIDIA driver the import
# succeeds and nvmlInit() fails — which `_gpus()` already treats as "no GPU"
# and falls through. Safe to install unconditionally; nothing here requires it.
nvidia-ml-py>=12.535

# Graph storage and traversal for Track 2 (app/services/knowledge_graph.py).
# MODULES.md §3.4 chose NetworkX over a git-tracked YAML file rather than a
# sixth SQLite store: the graph is tens of nodes, hand-authored, and edited
# rather than inserted into, so the file is the authoring surface and the store
# count stays at five. Pure Python, no C extensions, no network — Rule 1 clean.
networkx>=3.2

# Parses that graph file. It arrives transitively via chromadb-client, but
# knowledge_graph.py imports it directly and — as with httpx above — a
# transitive dependency is not a contract.
pyyaml>=6.0

# PDF text extraction for the ingestion pipeline (app/services/extraction.py).
#
# **Optional at runtime, by design.** `extraction.pdf_available()` probes for it
# and a machine without it still boots, still ingests .txt/.md/.csv/.json, and
# refuses a PDF with a message naming this package — the same optional-import
# discipline `vector_store` uses for Chroma and `hardware` for pynvml. Listed
# here so a container rebuild picks it up; nothing breaks before one happens.
#
# Pure Python, no C extensions, no network at import or at call. Rule 1 clean.
# There is deliberately no OCR dependency: a scanned PDF has no text layer, and
# the pipeline says so rather than storing a document whose chunks are blank.
pypdf>=4.0
